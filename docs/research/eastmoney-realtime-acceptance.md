# 东财实时行情验收记录

> 当前结果：`NOT_RUN`
>
> 自动化测试已验证契约、状态、凭据边界和无假数据降级，但不能替代真实 A 股交易时段
> 验收。在全部条件满足前，Phase 3A 保持 `IN_PROGRESS`。

| 检查项 | 当前结果 | 证据/备注 |
| --- | --- | --- |
| 测试日期与交易时段 | NOT_RUN | 待在 A 股连续竞价时段执行 |
| 东财终端版本 | NOT_RUN | 仅记录版本，不记录账号 |
| Python SDK / `gm` 版本 | NOT_RUN | 仅记录版本与是否可导入 |
| 六个核心指数覆盖 | NOT_RUN | 目标 6/6 |
| 行情更新节奏 | NOT_RUN | 记录聚合轮询结果 |
| 事件年龄中位数/最大值 | NOT_RUN | 仅记录毫秒聚合值 |
| 连续运行 30 分钟稳定性 | NOT_RUN | 检查内存与重复订阅 |
| 主动断开后的 `STALE` 检测 | NOT_RUN | 不记录原始行情 |
| 终端恢复后的自动重连 | NOT_RUN | 确认无假行情补位 |
| 解析错误数 | NOT_RUN | 目标为 0 |
| 仓库与日志凭据扫描 | NOT_RUN | 运行 repository policy |

## 通过门槛

六个指数均收到真实事件，首页点位、涨跌幅和时间戳与东财终端抽样一致；连续运行至少
30 分钟；断开后及时进入 `STALE`，恢复后重新收到真实事件；解析错误可解释且仓库、日志
无凭据。任何一项尚未执行时不得把结果改为 `PASSED`。

## 聚合探针结果

尚未运行。这里只允许粘贴 `tools.eastmoney_probe` 的聚合字段；禁止粘贴原始证券代码、
价格、行情响应、账号或 Token。
